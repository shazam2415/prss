# Provide the external URL to access your backend API
# If deploying on a server with Nginx and a domain, it might be: https://api.yourdomain.com/api
# Or if just using IP/Port locally or directly: http://YOUR_SERVER_IP:8050/api
VITE_API_URL=http://localhost:8050/api
